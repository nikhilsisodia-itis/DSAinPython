# Lists in Python are ordered and mutable collections of objects. It alsso allows duplicates. 
# They can contain any type of object, including other lists.
# Lists are defined by enclosing a comma-separated sequence of objects in square brackets ([]).
# One of 4 built-in data types in Python used to store collections of data.

# Example a list of integers:
numbers = [1, 2, 3, 4, 5]
print(numbers)
